# Processed Data (data/processed/)

Contains the final processed abundance matrices (rg_final.csv), alongside the cleanly normalized, log-transformed dataset (rg_final_clean.tsv) leveraged for downstream statistical modeling and visualization.
